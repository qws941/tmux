# Lock Threads
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _lock-threads.yml

name: Lock Threads

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    uses: qws941/.github/.github/workflows/_lock-threads.yml@master
