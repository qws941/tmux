# Issue Auto-Label
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _issue-label.yml

name: Issue Auto-Label

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  label:
    uses: qws941/.github/.github/workflows/_issue-label.yml@master
