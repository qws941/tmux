# Dependabot Auto-Fix
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _dependabot-auto-fix.yml

name: Dependabot Auto-Fix

on:
  workflow_dispatch:

permissions:
  issues: write
  security-events: read

jobs:
  dependabot-fix:
    uses: qws941/.github/.github/workflows/_dependabot-auto-fix.yml@master
    secrets: inherit
