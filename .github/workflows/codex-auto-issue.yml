# Codex Auto-Issue
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _codex-auto-issue.yml

name: Codex Auto-Issue

on:
  issues:
    types: [labeled]

permissions:
  issues: write

jobs:
  codex:
    uses: qws941/.github/.github/workflows/_codex-auto-issue.yml@master
    secrets: inherit
