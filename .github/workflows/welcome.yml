# Welcome First-Time Contributors
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _welcome.yml

name: Welcome

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome:
    uses: qws941/.github/.github/workflows/_welcome.yml@master
